//Export at least one string‑manipulation function using export syntax
export function concatenateStrings(str1, str2) {
    return str1 + str2; 
}

export function splitString(str, delimiter) {
    return str.split(delimiter); 
}
